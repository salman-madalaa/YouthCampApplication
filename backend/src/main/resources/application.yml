spring:
  application:
    name: youthcamp-api

  profiles:
    active: dev

  mail:
    host: smtp.gmail.com
    port: 587
    username: madalasalman95@gmail.com
    password: lfjhjfnjuiwcprnh
    smtp:
      auth: true
      starttls:
        enable: true

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    secret: ${JWT_SECRET:change-this-secret-key-for-production-youthcamp-2026}
    expiration: 86400000

logging:
  level:
    root: INFO

springdoc:
  api-docs:
    path: /api-docs

swagger-ui:
  path: /swagger
  operationsSorter: method
  tagsSorter: alpha